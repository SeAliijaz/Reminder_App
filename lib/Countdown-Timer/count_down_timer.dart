import 'package:flutter/material.dart';

class CountDownTimerScreen extends StatelessWidget {
  const CountDownTimerScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CountDownTimerScreen'),
      ),
    );
  }
}
